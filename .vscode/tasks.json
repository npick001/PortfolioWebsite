{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "cmake",
            "command": "build",
            "problemMatcher": "$msCompile",
            "label": "CMake: build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Runs the exact CMake Tools build command"
        }
    ]
}